using System;

namespace Nomad2.Sorting
{
    public enum PaymentSortOption
    {
        PaymentId,
        RentalId,
        CustomerId,
        CustomerName,
        BikeId,
        BikeModel,
        AmountToPay,
        AmountPaid,
        PaymentDate,
        PaymentStatus
    }
} 